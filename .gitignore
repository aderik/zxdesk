# tools holds pasmo, Fuse's ROM and the esxDOS image, none of which are
# ours to distribute; shots is screenshot output.
/tools/
/shots/
build/
__pycache__/
.DS_Store
*.zip

# private notes, not for the public repo
/docs/

# real MSX BIOS ROMs for the harness and run.sh, not ours to distribute
/msx/roms
